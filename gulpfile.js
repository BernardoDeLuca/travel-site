require('./gulp/tasks/styles');  //when using require, the .js extension is not necessary at the end of the file that you're requiring.
require('./gulp/tasks/watch');  //when using require, the .js extension is not necessary at the end of the file that you're requiring.


// The following dummy tasks are no longer needed.  They only demonstrated how gulp tasks work.

/*
gulp.task('default', function() {
console.log("Hooray - you created a Gulp task.");
});

gulp.task('html', function() {
console.log("Imagine something useful being done to your HTML here.")
});
*/
